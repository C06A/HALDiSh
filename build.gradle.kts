// group and version are defined in gradle.properties and apply to all projects automatically.
// Each subproject declares its own lifecycle tasks (clean, build, test, etc.)
// in its own build.gradle.kts file.
