
## HAL-DIscovering SHell scripts project

This project contains 2 modules:

* `haldish` module contains BASH shell scripts to do HTTP requests
to the server and to extract information from the response
* `example` module contains the sample scripts, using scripts from another module


Here is the example, accessing the list of Providers in apiGuru site
and the first Provider from the list.

[source,bash]
----
include::haldish/src/tests/scripts/apiGuruSpec.sh[tag=sampleApiGuru.sh]
----
